import { Typography, Container } from '@mui/material';

function About() {
  return (
    <Container key="about-container">
      <Typography variant="h3" color="black">
        This is about.
      </Typography>
    </Container>
  );
}

export default About;
